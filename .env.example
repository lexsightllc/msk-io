# MSK-IO Environment Configuration Template
# Copy this file to .env and customize the values for your environment

# =============================================================================
# Application Settings
# =============================================================================

# Logging configuration
MSKIO_APP_LOG_LEVEL=INFO
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL

# Log file path (optional, defaults to ./msk_io.log)
#MSKIO_APP_LOG_FILE_PATH=/var/log/msk_io/app.log

# Directory to monitor for new medical data input
MSKIO_APP_WATCH_DIRECTORY=/data/msk_io_input

# Directory for saving diagnostic reports and results
MSKIO_APP_OUTPUT_DIRECTORY=/data/msk_io_output

# Maximum number of concurrent pipeline tasks
MSKIO_APP_MAX_CONCURRENT_TASKS=5

# =============================================================================
# LLM (Large Language Model) Settings
# =============================================================================

# OpenAI API key for GPT models
#MSKIO_LLM_OPENAI_API_KEY=sk-your-openai-api-key-here

# Google Cloud API key for Gemini/PaLM models
#MSKIO_LLM_GOOGLE_API_KEY=your-google-api-key-here

# Default LLM model to use
MSKIO_LLM_DEFAULT_LLM_MODEL=gpt-4o
# Options: gpt-4o, gpt-4-turbo, gpt-3.5-turbo, gemma-2b, phi-2, etc.

# Timeout for LLM API calls (seconds)
MSKIO_LLM_LLM_TIMEOUT_SECONDS=60

# =============================================================================
# Image Processing Settings
# =============================================================================

# Path to segmentation model weights
MSKIO_IMG_DEFAULT_SEGMENTATION_MODEL_PATH=/models/nnunet/model_weights.pth

# Minimum confidence threshold for segmentation masks (0.0 to 1.0)
MSKIO_IMG_SEGMENTATION_CONFIDENCE_THRESHOLD=0.7

# Directory for caching processed images
# Defaults to system temp directory + msk_io_cache/images
#MSKIO_IMG_IMAGE_CACHE_DIR=/var/cache/msk_io/images

# =============================================================================
# Retrieval Settings
# =============================================================================

# Base URL for DICOM stream sniffing (e.g., Orthanc PACS)
MSKIO_RETRIEVAL_DICOM_SNIFF_URL=http://localhost:8042/dicom

# Run OHIF extractor in headless mode
MSKIO_RETRIEVAL_OHIF_EXTRACTOR_HEADLESS=true

# Directory for downloaded medical data
MSKIO_RETRIEVAL_DATA_DOWNLOAD_DIR=/data/msk_io_downloads

# Timeout for remote DICOM URL downloads (seconds)
MSKIO_RETRIEVAL_DICOM_SNIFF_URL_TIMEOUT_SECONDS=30

# =============================================================================
# Indexer Settings (Semantic Search)
# =============================================================================

# HuggingFace model for text embeddings
MSKIO_INDEXER_EMBEDDING_MODEL_NAME=sentence-transformers/all-MiniLM-L6-v2
# Options: sentence-transformers/all-MiniLM-L6-v2,
#          sentence-transformers/all-mpnet-base-v2, etc.

# Path to the FAISS vector database
MSKIO_INDEXER_VECTOR_DB_PATH=/data/msk_io_vectordb

# =============================================================================
# Security Notes
# =============================================================================
#
# IMPORTANT:
# - Never commit this .env file with real secrets to version control
# - Use strong, unique API keys
# - Restrict file permissions: chmod 600 .env
# - Only process PDFs and medical data from trusted sources
# - See SECURITY.md for more security best practices
#
# =============================================================================
